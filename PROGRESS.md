# Atlas GA Completion — Progress Ledger

Tracks the run-to-completion stages in
[docs/plans/ga-completion-plan.md](docs/plans/ga-completion-plan.md). One line per
stage at close-out (gate green + docs synced + committed).

| Stage | Status | Notes |
|---|---|---|
| M3 — migrations + hardening | ✅ done | Versioned migration runner (`schema_version` + ordered idempotent steps) folding old `_migrate()`; `scripts/backup.sh`, `run-prod.sh`, example systemd unit; `ATLAS_REQUEST_LOG` JSON request log; secure defaults confirmed; `scripts/check_migrations.py` added to gate; ops docs added. |
| M6 — government pack | ✅ done | `atlas/packs.py` (validate/import/export, reuses workflow + trigger validators); additive `/api/packs`, `/api/packs/import`, `/api/packs/{id}/export` (RBAC: read / workflows.manage); `atlas/packs/gov_complaint.json` (intake→triage→draft→human gate→publish) runs end-to-end on a mock worker; `scripts/check_packs.py` in gate; `docs/specs/pack-format.md` + openapi + api-reference EN/TH. |
| M4 — Atlas Fleet | ✅ done | New `fleet/` (own SQLite registry, no shared tenant DB, no tenant logic in core); `atlas-fleet` CLI provision/list/health/usage-pull; admin token by `admin_token_ref` + 0600 secrets sidecar (never raw token in registry/logs); compose IaC stub + systemd alt. Added additive unauthenticated `GET /healthz` (`{ok,service,version}`) to atlas core for health probes. `fleet/check_fleet.py` in gate (provision→register→health→usage-pull→offline). Docs: `fleet/README.md`, openapi + api-reference EN/TH (/healthz). |
| M5+B3 — CDR export | ✅ done | `fleet/cdr.py`: aggregate raw usage per tenant/period → deterministic CDR CSV (one file per tenant), `x-schema: atlas.cdr.v1-proposed` marker; `python3 -m fleet cdr --from --to --out-dir` (monthly+annual). Export only — no rating/invoices. `scripts/check_cdr.py` in gate (row counts, schema columns, byte-identical re-export). Doc: `docs/specs/cdr-schema.md`. |
| B2+B4 — usage view + alert | ✅ done | Dashboard **Usage** view (index.html/app.js/styles.css): from/to controls, run/job/budget totals from `/api/usage`, authenticated JSON/CSV blob downloads (no token in URL), gated to admin/auditor. B4 read-only run-count threshold alert (`usage_threshold_alert` in usage.py; client mirror) that never touches budget_units. All gate-marker substrings preserved; verified live in-browser (totals + tripped alert). `check_usage.py` extended; user-guide EN+TH updated. Codex 1×P2+1×P3 fixed (token-in-URL → blob fetch; load on restored view). |
| M8 — pack signing | ✅ done | `sign_pack`/`verify_pack_signature` (HMAC-SHA256 over canonical bundle, `ATLAS_SECRET_KEY`); import verifies a present signature (tampered/wrong-key/no-key rejected), unsigned accepted unless `require_signature`; `python3 -m atlas.packs sign/verify` CLI; `signed` flag in listing. Marketplace = readiness doc (Fleet-side, not core). `check_packs.py` extended; pack-format.md + openapi + api-reference EN/TH updated. |
| B5 + M7/B7 — BYOK / inference readiness | ✅ done | B5: `atlas/byok.py` write-only key injection (option-b env file 0600), audited, key never in Atlas DB/logs/responses; CLI reads key from `$ATLAS_BYOK_KEY` (never an arg); option-a (thClaws save-key) interface defined as a documented stub. `scripts/check_byok_helper.py` in gate (asserts key absent from DB file). M7/B7: `docs/specs/managed-inference.md` — gateway-worker + token/GPU-hour metering emits extra CDR rows; lives in worker/gateway layer, no Atlas-core change. Doc: `docs/specs/byok-key-injection.md`. |
| M9 — pooled-tenancy ADR | ✅ done | `docs/adr/0001-multi-tenancy-silo-vs-pooled.md`: silo decision, exact pooled change-list (tenant_id on every table, scoping layer, cross-tenant RBAC, per-tenant limits, Fleet export scoping), staged migration + risks + test strategy, revisit trigger. No `tenant_id` in core — proven and guarded by `scripts/check_silo.py` (in gate). Docs/ADR only; zero core/tenant code. |
| GA wrap — security + docs + green gate | ✅ done | Full-surface security review (per-stage codex + a holistic `codex review --base main`): auth/RBAC on every new route, no plaintext key/token in logs (request log path-only)/DB (BYOK key absent; fleet token by ref + 0600)/responses, `/healthz` leaks nothing, ATLAS_SECRET_KEY signing, BYOK no-key-in-core. 3 pack findings fixed (import reference validation; version round-trip; non-string role → clean 400). Canonical gate `scripts/gate.sh` green from a clean tree; docs/README links all resolve. |

## Input Adapter & Return Path

Tracks [docs/plans/input-adapter-return-path-plan.md](docs/plans/input-adapter-return-path-plan.md).

| Milestone | Status | Notes |
|---|---|---|
| IA-1 — envelope + provenance | ✅ done | Reserved `_meta` (`source`/`reply`) parsed/validated at the single `WorkflowRunner.start_workflow` choke point shared by both ingress paths (`/api/workflow-triggers/{id}/fire` and `POST /api/workflow-runs`); legacy payloads without `_meta` unaffected. `_meta.source` audited (`workflow_run.provenance`) against the run_id. New `atlas/outbound.py` (`resolve_outbound_target`): stdlib-only SSRF/allowlist guard for `reply.callback_url` (empty `ATLAS_OUTBOUND_ALLOWLIST` = disabled by default), shared as-is by OB-1. `scripts/check_input_adapter.py` added to gate. Docs: contract status updated, plan DoD ticked. |
| OB-1 — outbound delivery | ✅ done | `deliveries` table (migration 005, no `tenant_id`) + `create_delivery`/`list_deliveries`/`update_delivery` in db.py. `OutboundService` in `atlas/outbound.py`: signed body (HMAC-SHA256, same primitive as the usage export) sent via a pinned-IP `http.client` sender (closes the DNS-rebind TOCTOU gap between the allowlist check and the actual connect); bounded backoff to `ATLAS_OUTBOUND_MAX_ATTEMPTS` (default 5) then dead-lettered `failed`; missing `ATLAS_SECRET_KEY` or a blocked URL never sends. Subscribed to the existing `workflow_run_completed` emission in `WorkflowRunner._notify_run_completed`, after the run outcome is persisted — failure-isolated, same discipline as usage metering. Additive `GET /api/deliveries`, `POST /api/deliveries/{id}/retry`, `POST /api/workflow-runs/{id}/deliver` (new `deliveries.read` permission for operator/auditor visibility; mutations reuse `workflows.run`). `scripts/check_outbound.py` added to gate (mock receiver on loopback + allowlist). Docs: openapi.yaml + api-reference EN/TH (`Delivery` schema, new §14), contract + plan DoD ticked. |
| OB-1 hardening (round 2) | ✅ done | A second read-only audit found 4 more gaps, all fixed with structural kills (heuristics leave residue): **(P1)** the credential guard was a keyword denylist (`webhook_secret`/`code`/`#access_token` slipped past) — replaced with a **structural URL-shape rule**: a `callback_url` is a bare scheme+host+path; userinfo, ANY query string, and ANY fragment are rejected (the three places a secret hides), so the class is closed regardless of param name. Adapters route via `correlation_id`, authenticate via `X-Atlas-Signature`. **(P1)** reconcile racing live completion created two rows with different ids (receiver couldn't dedupe) — the automatic completion delivery now uses a **deterministic id** (`_completion_delivery_id(run_id)`) inserted with `INSERT OR IGNORE` (`db.claim_delivery` returns a `created` flag); only the winner drives the send, so a run has exactly one completion `delivery_id`. **(P2)** `ATLAS_OUTBOUND_TIMEOUT` only bounded the body drain, not the status/header read (a slow-trickle header held a thread ~indefinitely) — added a **total wall-clock watchdog** in `_send` that `shutdown(SHUT_RDWR)`s the socket at the deadline (a plain `close()` won't interrupt an in-flight `getresponse()`; the `HTTPResponse` fp keeps the fd open) and treats a response salvaged after the deadline as a `TimeoutError` (a slow *body*, status already read, still succeeds). **(P2)** reconcile scanned only the newest 10k runs — now complete via `db.iter_pending_delivery_ids()` (indexed on status, all ages) + `db.runs_missing_webhook_delivery()` (`json_extract` anti-join, only webhook runs missing a row). `check_outbound.py` + `check_input_adapter.py` extended (structural shape rejection incl. a keyword-denylist-evading param, deterministic-id convergence, slow-header total-deadline bound, complete reconcile); gate + lint green. |
| OB-1 hardening (round 1) | ✅ done | A read-only audit found 5 gaps, all fixed: **(P1)** `OutboundService.reconcile()` — restart recovery for a `pending` delivery whose attempt thread died, or a completed run that crashed before its delivery row was ever written; run in a background thread from `AtlasRuntime.__init__` (mirrors `reconcile_runs`/`reconcile_jobs`). **(P1)** `callback_url` embedding credentials (userinfo or a credential-shaped query key like `token`/`secret`/`api_key`) is rejected in `resolve_outbound_target` — never persisted into run input / the deliveries ledger, never returned via read APIs; adapters authenticate via `X-Atlas-Signature` instead. **(P2)** an exact-hostname allowlist match now always resolves DNS too (previously skipped resolution entirely), so the sender always pins the actual connection to a validated address, closing a rebinding gap for operator-trusted hostnames. **(P2)** response draining is bounded by bytes AND a wall-clock deadline using `read1()` (not `read()`, which loops internally trying to fill the buffer — same pitfall already documented on `iter_sse` in `thclaws_client.py`); a receiver that trickles bytes can no longer hold a delivery attempt open indefinitely. **(P3)** `delivery.blocked`/`delivery.delivered`/`delivery.failed` audit entries added (previously only `delivery.create` existed). `scripts/check_outbound.py` + `scripts/check_input_adapter.py` extended (credential rejection, reconcile, bounded-drain timing, audit entries); gate + lint still green. |

## thClaws API Adoption

Tracks [docs/plans/thclaws-api-adoption-plan.md](docs/plans/thclaws-api-adoption-plan.md).

| Milestone | Status | Notes |
|---|---|---|
| T0 — worker contract spike | ✅ done | Rebuilt and probed thClaws `0.85.0` source `18e3aa2`: `/workspace/sync/stat` returned 200 with and without the worker Bearer on loopback and a `0.0.0.0` listener, while `/v1/deploy/manifest` returned 401 without Bearer and 200 with it. Added `docs/specs/thclaws-worker-contract.md` with the endpoint/auth matrix, exact tested build, SSE event contract, 409-busy semantics, multiuser outer-HMAC requirement, and persistent `sync_mode` gate (`disabled` default; only an authenticated admin pre-enable probe may call sync while disabled). Deployment guidance and docs index updated. Filed upstream asks as thClaws discussions [#178](https://github.com/thClaws/thClaws/discussions/178) and [#179](https://github.com/thClaws/thClaws/discussions/179). Full gate + lint green; Codex review findings (multiuser auth, enforce enable-time probe, allow the pre-enable probe while disabled) fixed; final review clean. |
| T1a — token usage capture | ✅ done | `extract_usage()` in `thclaws_client.py` (mirrors `extract_session_id`: tolerant, `None` on anything malformed — strings/bools/negatives/over-64-bit ints/non-dict/non-JSON — never raises). Job stream loop captures usage with a per-key last-seen merge (a partial late frame can't clobber counts already seen); `_record_job_usage` passes `tokens_prompt`/`tokens_output` into the existing `usage_events` columns with the full payload (cached/creation/reasoning) under `metadata.measures`; `byok_token_counts_billable: false` untouched. `summarize_usage` gains additive `tokens_prompt`/`tokens_output` totals (openapi `UsageTotals` + api-reference EN/TH updated). Old worker → NULL tokens, job succeeds; malformed usage frames tolerated with the ledger row intact. Checks in `check_usage.py`/`check_jobs.py`, 5 mutations proven red (extract_usage→`{}`; NULL→0 default; dropped type guard; dropped 64-bit bound → OverflowError loses the ledger row; merge→overwrite). Stale "No token/usage capture" gap in `usage-metering-billing-plan.md` corrected (thClaws emits usage since v0.85.0). Codex rounds: 2×P2+1×P3 found and fixed, final pass clean. |
| T2 — structured event surfaces | ✅ done (run-view rollup deferred) | **Parser fix:** `extract_text` scoped to assistant-text frames only (`text`/legacy `message`/OpenAI-compat `delta`/`content`); named structured events (`thinking`, `user_message_injected`, `tool_*`, `skill_*`, `usage`, `result`, `error`) now fall through to `append_job_event` instead of being folded into `assistant_text`. **Projection:** `project_structured_event()` whitelists tool/skill events to `{id, name, status, input_bytes, output_bytes, input_sha256, output_sha256}` before storage — raw `input`/`output` never reach SQLite (a payload can carry a BYOK key Atlas can't detect; truncation ≠ redaction); `status` honors the worker's own value (thClaws sends `{"status":"error"}`), length-capped, else derived. Applied in `jobs.py`; a session-tagged tool frame is still stored (not dropped). **Read-time redaction:** `redact_tool_payload_for_read` in `_stream_job_events` (`app.py`) projects any legacy raw tool/skill row (keys outside the structural allowlist) on read, so old DBs never leak raw payloads to the dashboard/API. **Output cap:** moved to `iter_sse(max_total_bytes=…)` — bounds cumulative RAW wire bytes at the source (data, framing/whitespace padding, comment/data-less frames, terminal frame all count), closing every padding/bypass vector the parser change opened. **Dashboard:** new per-job Timeline tab — client-side `buildToolTimeline()` pairs start/result by id, derives status/duration from structural metadata alone, per-job counters (tools/denied/failed); worker-controlled names escaped + length-capped; dispatcher surfaces every frame (known + unknown) so unknown events render generically and never crash. Threat model updated (worker row + invariant-match rows: payloads never persisted incl. read redaction; total output bounded). Checks: `check_jobs.py` (parser scoping incl. delta/content, secret byte-scan, session-tagged store, worker-status honor, 5 output-cap flood vectors, legacy read-redaction e2e over HTTP) + new `check_event_views.py` (node-driven timeline builder + name escaping + dispatch markers). 15 mutations proven red across the two check files. Codex review: ~10 P2 rounds (output-cap evasion via session-skip / event-name / terminal / parse-order / whitespace / data-less-comment; legacy-read exposure incl. non-input/output fields; delta/content compat; worker-status misclassification) all fixed + mutation-locked, final pass clean. **Deferred:** run-view per-node counter rollup — needs a bulk per-node events aggregate no API provides; no mandatory check covers it (see plan). |

| T3 — async execution via x_callback | ✅ done | **Client defect fixed:** `x_callback` was a dead `str` param; now `run_agent_async()` sends the real thClaws OBJECT envelope `{url, api_key, run_id}` (idempotency = run_id = job id) and parses the 202 ACK (session bound from it). **New inbound surface:** `POST /api/worker-callbacks/{job_id}` — the ONE documented pre-auth carve-out (threat-model row added): dispatched in `_dispatch` before `_is_authorized()`; dedicated handler does body-cap-before-read (4 MiB, connection closed on reject), constant-time HMAC verify of the per-dispatch signed token (binds job id + expiry = deadline + worker's 0/10/60s×30s retry envelope + skew margin, `CALLBACK_RETRY_ENVELOPE_SECONDS=300`), audits as `system:worker-callback`, token never logged/stored (byte-scan checked, incl. dispatch-error echo redaction). **Idempotent terminal convergence (class fix):** `db.apply_job_terminal_result` — ONE transaction applies the atomic non-terminal→terminal UPDATE (whose `CASE` honors a racing `cancel_requested` as `cancelled`) PLUS all result-derived rows (summary text, job events, audit, usage ledger); callback, reaper, and dispatch-failure paths all use it, so duplicate delivery / callback-vs-reaper / cancel-vs-callback converge to ONE terminal state + one usage row, and a crash or write failure mid-apply rolls back the terminal state so the worker's retry re-applies the full result instead of losing it behind an already-terminal job. **Reaper** (`ATLAS_CALLBACK_TIMEOUT_SECONDS`, deadline written BEFORE the worker POST so a crash can't orphan the job) + `reconcile_jobs` exemption keeps callback-pending jobs alive across restarts (late callback still lands; orphaned stream jobs still fail). **Ambiguity split:** definitive rejection (worker 4xx / connection-refused / DNS) fails the job; ambiguous failures — reset/timeout after send, 5xx (a proxy 502/504 or worker 500 can arrive AFTER the run was scheduled), oversized/undecodable ACK — leave it callback-pending (`callback_dispatch_unconfirmed` event) so the real result is never discarded; post-ACK bookkeeping errors can't fail an accepted run; the ACK read itself is bounded (64 KiB + wall-clock deadline, chunked like `iter_sse`) so a hostile 2xx body can't pin the dispatch thread or exhaust memory. **Unauthenticated-surface bound:** rejected callbacks write a durable `job.callback_rejected` audit row ONLY for real job ids AND at most once per job per window — neither junk requests nor a compromised worker holding real ids can grow the DB/WAL. **Workflow start gate:** `POST /api/workflow-runs` rejects a graph with callback nodes synchronously (400, no run row) when `ATLAS_PUBLIC_BASE_URL`/`ATLAS_SECRET_KEY` are unset. **ACK contract:** only a conforming 202 ACK (status `accepted` + echoed run id) records a dispatch — a non-202 2xx (incompatible worker/proxy) fails fast, a mismatched ACK stays pending and never binds a session from the untrusted echo. **Payload validation:** a delivered `run_id` mismatching the URL's job → 400 untouched (mix-up can't apply B's result to A); non-string `status` maps to failed (never TypeError→500); explicit `null`/falsey `execution` → 400 (default only on absence). **Workflow attribution + waits:** the node→job link repairs NULL usage attribution in the same transaction (fast-finish race), the runner's wait extends to a callback job's own deadline (long callbacks aren't cancelled at the fixed cap), and dispatch-failure metering refetches the started row (no NULL start/duration). **Opt-in:** `execution: "callback"` per job + workflow worker/manager nodes (schema updated); default-only-on-absence, falsey/unhashable values → clean 400; rejected at submit without `ATLAS_PUBLIC_BASE_URL`/`ATLAS_SECRET_KEY` (no orphan conversation). Workflow-run restarts keep the standing explicit-recovery rule; recovery entries flag `callback_pending` jobs + operator warning (retry always submits a NEW job). Usage reuses T1a parsing (callback `usage` → `extract_usage`); dispatch thread records usage ONLY when it terminal-izes (race found during mutation testing: its NULL-token row could shadow the callback's real counts via idempotent-insert ordering). Docs: openapi (path + `callbackToken` scheme + `WorkerCallbackPayload` + `JobRequest.execution`/`Job` fields), api-reference EN/TH §Jobs+async section, concepts EN/TH job-modes paragraph, deployment env table, threat model, workflow-definition.schema.json; `check_docs.py` route extractor widened to `_dispatch` (carve-out discoverable + sanity floor). Checks: new `scripts/check_async_jobs.py` (49 checks, hermetic, in gate). **58 mutations proven red** (A carve-out removal→401; B skip signature; C reconcile exemption; D body cap; E token envelope; F non-atomic terminal write; G reaper comparison; H submit preconditions; I isinstance guard; J Config timeout plumb; K token echo redaction; L recovery flag; M ambiguous-fail; N cancel-CASE; O falsey coercion; P split-transaction apply; Q 5xx-as-definitive; R unbounded ACK read; S unconditional rejection audit; T workflow start gate dropped; U non-202 ACK accepted; V ACK fields unvalidated; W run_id guard dropped; X status isinstance dropped; Y null coerced; Z audit rate limit dropped; AB usage backfill dropped; AC stale metering snapshot; AD wait extension dropped; AE body-read deadline dropped; AF unlocked audit throttle; AG in-txn attribution dropped; AH reaper index dropped; AI index DDL in base SCHEMA → legacy migration crash; AJ read-slot gate dropped; AK cap-clear fail-open; AL state predicate dropped from the index; AM 408 non-retryable; AN error-body bound disabled; AO terminal token not redacted; AP handoff not resumed on replay; AQ uncapped ACK socket timeout; AR ACK session_id token-echo not skipped; AS terminal worker-field token not redacted; AT handoff claim not serialized; AU error-body read no deadline; AV run_id lenient; AW non-deterministic handoff id; AX no existing-child recovery; AY reaper ignores retry grace; AZ public body id honored; BA error-body read no per-tick cap; BB HTTPException ACK not ambiguous; BC callback slot released before apply; BD workflow-wait helper ignores grace; BE error-body reader lets HTTPException escape; BF callback reject doesn't close connection; BG replay ignores handoff_error). Codex rounds: R1 1×P1+3×P2, R2 3×P2+1×P3, R3 4×P1+1×P2 (atomic apply; 5xx ambiguity; bounded ACK; audit DoS bound; workflow start gate), R4 1×P1+4×P2 (per-job audit rate limit; usage-attribution backfill at link; metering refetch; callback-aware workflow wait; run_id cross-check — plus operator-pasted set re-verified: ACK 202 contract + null execution closed, rest already fixed), R5 2×P1+2×P2 (inbound body read bounded in time — per-recv timeout + deadline via read1 chunks; audit throttle check-and-reserve under a lock; workflow attribution derived IN the terminal transaction, closing the last interleaving; partial index `idx_jobs_callback_due` so the reaper sweep is an index lookup — migration 007), R6 3×P1+1×P2 (R5's SCHEMA-placed index crashed LEGACY pre-schema_version migrations — index now lives only in step 007, after the columns exist; concurrent body reads slot-bounded with retryable 503 overflow; audit cache saturation fails CLOSED instead of clearing all windows; index predicate carries the state filter so terminal jobs leave the index), R7 2×P1+3×P2 (body-read timeout returned a non-retryable 408 — thClaws abandons on non-429 4xx, so it now returns a retryable 503; the shared `_request` error path read the worker error body unboundedly — now capped at 64 KiB; a worker error message reflecting the callback token was persisted verbatim — now redacted; a crash between the terminal commit and the handoff left the source succeeded with no child — a successful callback replay now resumes the unstarted handoff idempotently; the ACK-read socket timeout could exceed the wall-clock deadline when `ATLAS_REQUEST_TIMEOUT` > 60s — capped at the deadline), R8 2×P1 (round-7's error.message-only redaction was incomplete — the callback token is a LIVE credential a worker can echo into session_id / summary / tool names; a single `_redact_token` helper now scrubs EVERY worker-controlled persisted field and a token-carrying ACK session_id is skipped, not bound; round-7's replay-handoff recovery had a check-then-submit race that two concurrent duplicate callbacks could both pass, spawning duplicate children — the claim is now serialized under the manager lock with a re-read, same one-writer-per-DB basis as claim_trigger_dedupe), R9 2×P1+1×P2 (round-8's byte-cap on the dispatch error body still let a slow-drip body pin the thread — now a chunked read under a wall-clock deadline; round-8's in-process handoff lock could not close the crash window between `submit()` and the `handoff_job_id` write — the child now has a DETERMINISTIC id derived from the source, so a retry after that crash LINKS the existing child instead of creating a second one; `run_id` validation now requires an exact nonempty-string match to the URL job, rejecting missing/null/empty/non-string instead of accepting them), R10 1×P1+2×P2 (the reaper fired at the raw deadline while the callback token stayed valid CALLBACK_RETRY_ENVELOPE_SECONDS longer — a retryable-503/timeout retry near the deadline lost its result; the reaper now waits deadline + that grace, matching the token lifetime; round-9's deterministic child id became a hijack vector once `submit()` read `id` from the request body — `POST /api/jobs` passes the body straight through, so a caller could occupy a source's handoff id; the id now flows only through a PRIVATE keyword-only `explicit_id`; round-9's error-body deadline was checked only between reads, so one withheld read could overrun it by a whole socket timeout — each recv is now capped at the remaining deadline via the response socket), R11 3×P1 (round-6's ambiguity split missed protocol-level ACK failures — `http.client.IncompleteRead`/`BadStatusLine` are HTTPException, not OSError, so a post-202 protocol error was treated as definitive and lost the result; now caught as ambiguous; round-4's workflow-wait extension used a fixed +120s, less than round-10's 300s reaper grace, so a valid late retry could be cancelled by the runner — the wait now derives from the job service's OWN reap grace (single source of truth) via a pure, unit-tested helper; round-6's callback read-slot was released right after the read, leaving the JSON decode / token scans / DB apply unbounded — the slot is now held through the entire read+parse+apply so concurrent PROCESSING is bounded), R12 1×P1+2×P2 (round-9's `_read_error_body` still let `http.client.IncompleteRead` (a truncated chunked body — HTTPException, not OSError) escape its 'never raises' contract; now caught; the callback route's pre-body-read rejections (missing/invalid Content-Length) returned 400 WITHOUT closing the connection, so unread body bytes could desync a keep-alive/proxy connection into the next request — the handler now closes the connection on every reject path; round-7's replay-handoff recovery re-ran on every duplicate callback even after the handoff was SKIPPED/FAILED (handoff_error set, handoff_job_id null), appending an event/audit row each time — a token-holder's unbounded-write vector; recovery now fires only when the handoff genuinely never resolved) — all fixed + mutation-locked; final pass clean. R6 also caught a harness bug of this run: `gate.sh | tail` inside `&&` masked the gate's exit code — verification now runs the gate unpiped. |
| T4 — advisory worker state + info surface | ✅ done | Persistent `workers.sync_mode` enum column (migration 009), NOT the poll-rewritten `agent_info` blob; operator-owned setter + `worker.sync_mode_changed` audit (old→new + actor). Admin `POST /api/workers/{id}/sync-mode` runs a pre-enable `sync_stat()` probe through the same client path — a failed probe returns 400 and leaves the mode unchanged (the only caller allowed to probe while `disabled`). Poll probes `/workspace/sync/stat` ONLY when `sync_mode != disabled` (sync/* is not Bearer-protected); error → `busy: null`, never carried forward (unlike the pricing catalogue), never a routing blocker; non-bool → null. Router: `busy` false>null>true as a PURE tie-break (byte-identical when null everywhere via stable sort) + a skill-hint bonus strictly below any tag/role weight (guarded on skill data, existing fixtures don't flip). Dashboard: `safeHttpUrl` renders the `external_access.ui_url` link only for http/https (js:/file:/data: rejected), escaped + `rel=noopener`; busy badge with staleness, admin sync-mode select (reload-on-reject), skills marked "daemon-scoped, advisory", version-mismatch warning. New `scripts/check_worker_state.py` (hermetic: migration/enum-validation, sync_mode-survives-poll, gated busy probe, router tie-break, fixture stability, skill-hint ceiling, full HTTP enable-probe path + audit actor, node scheme+version guards) in gate; **4 mutations proven red** (invert tie-break / skip enable-probe / sync_mode-in-agent_info / probe-when-disabled). openapi + api-reference EN/TH additive. Independent Claude review: SHIP-READY — 7 properties confirmed + 7 reviewer mutations red; 2 Low test-nits closed (audit-actor assert, version-predicate node test). Gate + lint green. |
| T5 — selective file collection (sync/export) | ✅ done | New `atlas/sync_files.py` — the **single** worker-tar validator (`safe_extract_tar`): rejects absolute / `..` / backslash paths and any non-regular member (symlink/hardlink/device/fifo), enforces `ATLAS_SYNC_MAX_FILES`/`ATLAS_SYNC_MAX_BYTES` caps while streaming (bounds a decompression bomb), and `store_bytes` writes each extracted file only under a fresh OPAQUE id (never a member-derived path). Client `sync_export()` (`thclaws_client.py`): POST `/workspace/sync/export` with a JSON path array, response bounded in bytes (`max_bytes`) AND wall-clock (`deadline`) via the existing `_read_bounded`; bounded 409-`workspace busy` retry with a fixed delay, never past the deadline. **Pre-terminal collection barrier** in `jobs.py::_run` between `[DONE]` and the `succeeded` write (so handoff/workflow-node progression always sees a settled artifact set): resolves `collected`/`collection_failed`/`collection_skipped`, deadline-bounded by `ATLAS_COLLECT_DEADLINE_SECONDS`, **failure-isolated** (never changes the job outcome), audited with COUNTS only. Each file stored as a `file_ref` artifact (`run_id`+`job_id`, key `files.<node_key>.<relpath>` in a workflow else `files.<relpath>`, metadata relpath/sha256/size/source). Gated on T4 `sync_mode` (a `disabled` worker is never called → `files.collection_skipped`); absent `collect_files` = zero sync calls. Opt-in per job (`POST /api/jobs` additive `collect_files`) and per workflow worker/manager node (validated at graph-save time via the shared `_validate_collect_files`); rejected with `execution:"callback"` (barrier is on the stream path). Migration 010 (`jobs.collect_files`). New `scripts/check_file_collection.py` in gate (10 hermetic cases: byte-identical happy path, zero-call/skip gates, hostile-tar + symlink rejection, cap abort, 409-retry + persistent-409 give-up, deadline, barrier-vs-handoff ordering); **3 mutations proven red** (disable `..` rejection → hostile tar collected; move barrier past succeeded → handoff precedes collect; probe a disabled worker). openapi + api-reference EN/TH additive (`JobCreate.collect_files`, `Job.collect_files`, §File collection); threat-model gains the worker-supplied-tar trust-boundary row. Gate + lint green. |
| T6 — file handoff (sync/push) | ✅ done (single-job `handoff_push_files` deferred) | A workflow edge pushes previously-collected artifacts (T5) into the NEXT worker's workspace before the downstream job starts, so Coder→Reviewer / Reporter→Anchor chains hand over real deliverables. `sync_files.build_push_tar` — REPRODUCIBLE gzip tar (sorted members, normalized mtime/ids, gzip mtime=0) so the same file set hashes identically for audit. Client `sync_push()` (`thclaws_client.py`): POST /workspace/sync/push with a raw gzip body (new `_request` raw-body path), ACK bounded, bounded 409-busy retry (the 409-retry loop factored into shared `_call_with_409_retry`, reused by sync_export). **Additive by construction:** Atlas builds every arcname as `incoming/<run_id>/<node_key>/…` and NEVER sends trash/replace/delete, so a push can't clobber the target's files. **Opt-in:** edge `push_files` (artifact-key globs) requires `policy.file_handoff` — save-time validator (`validate_workflow_graph`) AND runtime guard (the `_execute_run` node loop: `do_push = bool(pushes) and policy.file_handoff`). Engine: a taken push_files edge stashes intent keyed by the target node; at the node's run time `_push_files_to_worker` resolves the globs against the run's `file_ref` artifacts, reuses the T5 caps + the target's `sync_mode` gate (disabled target → edge fails loudly), builds the tar, pushes to the RESOLVED worker, then audits `files.pushed` (count/bytes/sha256/target) before the downstream job. Downstream prompt `{files_dir}` (no dot → left by render_prompt's regex) substituted to the incoming prefix. A push failure raises → node fails → edge fails loudly (continue-on-failure records it). New `scripts/check_file_handoff.py` in gate (two mock workers A/B: end-to-end byte-identical push + {files_dir} substitution + trash-never-called; save-time validation; runtime guard; push-failure-fails-edge); **2 mutations proven red** (drop the runtime file_handoff guard → no-policy push reaches B; remove the save-time cross-check → push_files without policy no longer rejected). Docs: workflow-definition.schema.json (node collect_files [T5 backfill], edge push_files, policy file_handoff) + api-reference EN/TH (§File handoff) + threat-model Atlas-writes-to-workers row. **Deferred:** the single-job `handoff_push_files` variant (jobs.py) — no mandatory check covers it and standalone-job handoff needs its own opt-in + job column; workflow edges are the tested primary path. Independent Claude review found 2: **(critical)** `file_ref` artifacts can also be created via `POST /api/artifacts` with an arbitrary content path/relpath (not only T5 collection), so `_push_files_to_worker` now RE-VALIDATES every artifact — `_download_artifact`-style upload-store containment on `content` (no host-file exfiltration) + `_reject_unsafe_path` on `relpath` (no arcname escape), caps on ACTUAL bytes — with a hostile-artifact check + mutation; **(important)** the blocking network push moved OUTSIDE the shared workflow-runner lock (prepare under lock → push unlocked → submit under lock, worker pinned in the payload), mirroring the unlocked `_wait_for_job`, so it can't stall other runs' stepping or operator pause/cancel; dead `_submit_worker_node` removed. Re-verified gate + lint green. |

| Dashboard surfaces for T1/T3/T5/T6 | ✅ done | Surface on the web UI the features earlier milestones added only to the API/CDR. **T1a/T1b:** Usage view gains token totals + estimated (non-billable, labelled) cost metrics (`#usageTokens`/`#usageEstCost`, rendered in `loadUsage` from the existing `/api/usage` totals). **T3/T5:** Start-a-Job form gains a `collect_files` input + an async `execution:"callback"` toggle (wired into `submitJob`). **T6:** visual builder gains a `policy.file_handoff` toggle (a default-OFF `boolean_off` policy field — distinct from stop_on_first_failure's default-ON, so a missing key renders UNCHECKED) + an edge `push_files` input (`addBuilderEdge`); the run timeline renders `files_pushed` detail (count/bytes/target) inline instead of raw JSON. No `/api/*` change (pure frontend over existing endpoints), so no openapi/api-reference delta. New `scripts/check_dashboard_surfaces.py` in gate (marker + wiring assertions); mutations proven red (flip the file_handoff default-off read → silently enables handoff; drop the collect_files wiring). Existing dashboard gate markers preserved. Gate + lint green. |

| T1b/T5/T6 hardening round 2 (independent bug-hunt) | ✅ done | Five confirmed findings fixed, each with a mutation-proven check. **T5 atomic publication:** `_collect_files` now stages every blob + row and commits the artifact rows in ONE transaction (`db.create_artifacts`, validation refactored into `_artifact_row`); a mid-list failure publishes nothing and reclaims staged blobs (was: per-file `create_artifact` left earlier files live for T6 globs after `files.collection_failed`). **T6 cancel race:** the node-submit path re-checks the run state UNDER the lock after the unlocked push — a cancel landing during the push window no longer creates the downstream job at all (was: create-then-reap via `_wait_for_job`). **Client bounds:** `get_json`/`get_text` (healthz / agent info / models) now read via `_read_bounded` with an 8 MiB cap + monotonic deadline (was: bare `response.read()` — a drip-fed body reset the socket timeout per chunk and could pin a poll thread or exhaust memory). **T1b cost semantics:** `_estimate_cost_usd` prices the wire fields by their VERIFIED semantics — `prompt_tokens` arrives already uncached (openai_responses.rs subtracts `cached_tokens`; Anthropic's `input_tokens` natively excludes cache reads) and `completion_tokens` includes reasoning — so prompt is priced as-is (no cache double-discount, was undercounting cached runs) and reasoning is carved out of completion (no double-count, was overcounting; $0.000462 → $0.000446 on the check fixture). Semantics pinned in `docs/specs/thclaws-worker-contract.md` §usage; deliberately NOT a port of thClaws' own `compute_cost_usd`, whose TokenUsage convention contradicts what its api_v1 layer emits. **Unbounded enumeration:** new `db.iter_artifacts` (rowid-keyset paging) replaces every fixed artifact window — the T6 push glob resolution (was: hard-fail at ≥1000) and the per-job/per-run artifact routes (was: silent 1000-row truncation). 5 new/updated checks across check_file_collection/check_file_handoff/check_usage; 5 mutations proven red. Coverage sweep after: 3 more checks close the remaining mutation holes — the round-1 budget-gate-BEFORE-push ordering (was fixed in #31 with no lock), `create_artifacts` all-or-nothing at the db level, and the `get_text`/health drip bound. 8 mutations total. Gate + lint green. |

| T5/T6/client hardening round 3 (independent bug-hunt on round-2 commit) | ✅ done | Four more confirmed residues from reviewing the round-2 fix, each mutation-proven. **T5 audit atomicity:** `create_artifacts` now writes the artifact rows AND their `artifact.create` audit rows in the SAME transaction (`_AUDIT_INSERT`/`_audit_values` extracted so both `audit()` and the batch share one INSERT); previously the audit ran as a separate per-row transaction AFTER the artifact rows committed — an audit write failing there would leave live rows while `_collect_files` reclaimed the staged blobs, i.e. rows pointing at deleted files. **T6 deadline before submit:** the node-submit path now `_check_deadline(deadline)` both before the unlocked push and again UNDER the lock before `_reserve_and_submit_job`; a run whose `max_minutes` expired during the push no longer creates + submits the downstream job (was: create → run → the deadline only caught later at `_wait_for_job`). **T6 cancel node state:** the under-lock cancel guard raises `_WorkflowCancelled` (not a generic `ValueError`), so a cancel-during-push lands the node `cancelled` with no `node_failed` event (was: generic error → node `failed`). **Client open-phase bound:** new `_urlopen_deadline` gives every deadline'd worker call (`get_json`/`get_text`/`sync_export`/`sync_push`/`run_agent_async`) a wall-clock bound over the CONNECT + STATUS-LINE + HEADER phase, closing the one hole the round-2 body bound missed — a worker dribbling header bytes reset urllib's per-recv timeout forever and pinned the poll/collection/dispatch thread. A watchdog `threading.Timer` `shutdown()`s then `close()`s the captured socket at the deadline (a bare `close()` does NOT wake a blocked recv — POSIX); TLS context + hostname check are forwarded so capturing the connection can't drop cert verification; the streaming `run_agent_stream` deliberately gets NO open deadline (its lifetime is `stream_deadline` inside `iter_sse`). 4 new/extended checks; 4 mutations proven red. Gate + lint green. **Pre-existing flake noted (NOT from this change):** `check_async_jobs` can trip a teardown race — the `start_callback_reaper` daemon (`while True`, no stop) sweeps `list_due_callback_jobs` after the temp DB is torn down (`no such table: jobs` + `Directory not empty`); passes in isolation, intermittent under full-gate load. Needs a reaper stop-event or a long test interval; deferred as orthogonal. |

## External confirmations still outstanding

- **CDR record schema** — proposed (`x-schema: atlas.cdr.v1-proposed`); confirm fields/units with NT billing/mediation.
- **thClaws save-key endpoint** — BYOK option-a is a documented stub; blocked on thClaws shipping the endpoint (option-b env injection works now).
- **Provisioning target** — default docker-compose/systemd on a VM; GDCC/k8s noted as alternates (NT infra).
- **Auth SSO/OIDC** — local users now; OIDC is a documented extension point (NT IdP).

## Security findings

No open findings. Everything codex surfaced across the run was fixed and guarded by a hermetic check (see per-stage rows). Two intentional exceptions to per-user auth exist, both documented in `docs/specs/threat-model.md`: `GET /healthz` (unauthenticated, returns only `{ok, service, version}`) and `POST /api/worker-callbacks/{job_id}` (T3 pre-auth carve-out, authorized by its own per-dispatch HMAC token with body cap, idempotent apply, and system audit actor).
